function toggleMode() {
    const html = document.documentElement
    html.classList.toggle("light")
  
// nesse comando, toda vez que o botão for clicado (onclick no html), ele vai procurar no JS essa função do toggleMode 
  }